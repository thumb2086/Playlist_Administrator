[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "playlist-administrator"
version = "1.7.0"
description = "Music library management & Spotube automation"
requires-python = ">=3.7"

[project.scripts]
playlist-admin = "cli:main"
